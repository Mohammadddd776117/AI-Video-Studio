[project]
name = "ai-video-studio-backend"
version = "0.1.0"
description = "Backend service skeleton for AI Video Studio"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
