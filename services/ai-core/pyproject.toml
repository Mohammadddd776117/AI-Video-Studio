[project]
name = "ai-video-studio-ai-core"
version = "0.1.0"
description = "AI Core service skeleton for AI Video Studio"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
