[project]
name = "ai-video-studio-video-engine"
version = "0.1.0"
description = "Video Engine service skeleton for AI Video Studio"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
